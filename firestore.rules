rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Allow all reads and writes for testing
    // TODO: Replace with secure rules after testing
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
